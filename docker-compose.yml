services:
  mongodb:
    image: mongo:7
    container_name: leo-json-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root123
      MONGO_INITDB_DATABASE: leo_json
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
